const numberA: number = 2;
const numberB: number = 3;
console.log(numberA + numberB);

/**
 * In order get compilation errors we may run tsc by ourselves. It would 
 * require to install some development tools like npm-run-all and extra 
 * scripts setup in package.json.
 * 
 * npm install vite-plugin-checker --save-dev
 */